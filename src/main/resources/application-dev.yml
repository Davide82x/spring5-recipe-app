spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_dev?useSSL=false&allowPublicKeyRetrieval=true
    username: sfg_dev_user
    password: davide
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
 #   properties:
 #     javax:
 #       persistence:
 #         schema-generation:
 #           create-source: metadata
 #           scripts:
 #             action: create
 #             create-target: dv_database_create.sql